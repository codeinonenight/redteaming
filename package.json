{
  "name": "model-redteam-vercel",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 3000",
    "start": "python3 -m http.server 3000",
    "build": "mkdir -p public && cp -R index.html pages public/"
  }
}
